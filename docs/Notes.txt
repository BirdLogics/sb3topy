TODO List
Global variables
Reverse initial layering
Rotation styles
Sounds
When key pressed
Custom blocks
Clones
Pen
Variable reporters
List reporters
GUI
EXE conversion

Options to add:
FPS, Turbo, Warp time, Work time
Allow fullscreen, Allow fullscreen
Safe operators, Cast values
Mixer settings
DirtySprite timing treshold

=================================================
Unique naming


Target Names
Target Functions
    Event handlers
    Custom blocks
Target variables
Target lists

A target has:
    Functions
    Variables
    Lists

{
    General_Identifier: 
    (
        Iterator, # a-z, used to add items to included dict
        {
            Original_Identifier:
                (
                    Iterator # 1-3 # Used to create additional unique names
                    Modified_Identifier
                )
            }
        }
    )
}

broadcast@1
broadcast1
{
    "broadcast1":
    (
        iterator,
        {
            "broadcast@1": (iterator, "broadcast1"),
            "broadcast1: (iterator, "broadcast1_A")
        }
    )
}


general_identifiers = {
    identifier: iterator_az
}

specific_identifiers = {
    identifier: (cleaned, iterator_19)
}

modified_identifiers = {ident1, ident2, ...}

# Step 1, check if in specific_identifier
# If not, combine cleaned and iterator_az
# Step 2, combine modified and iterator_19
# Step 3, verify not in modified_identifiers



Already Named dict: {
    "custom block proccode %s": ["customBlock", 1],
    "greenFlag": [""]
}

Names List: [
    "customBlock",
    "greenFlag",
    "greenFlag2"
]

iter = itertools.permutations([''] * 3 + list("ABCDEFGHIJKLMNOPQRSTUVWXYZ"), 4)
for i in iter:
    print(''.join(i)) # A, B, C, ... AA, AB, AB, etc. 18278 possibilities




=================================================
Input Types

[1, Value] Wrapper for other values
[2, BlockId] Substack, condition
[3, Value1, Value2] Input covered /w block(s)

[4, Float] Most numbers
[5, UFloat] Time wait
[6, UInteger] Letter of, repeat x
[7, Integer] Go back layers, list item
[8, Float angle] Point in direction, angle menu
[9, Hex color] Pen color, touching color
[10, String] Most strings, can be # type for set var

[11, Name, BroadcastId] Broadcast blocks
[12, Name, VariableId, (x), (y)] Variable reporter
[13, Name, VariableId, (x), (y)]

=================================================

Scratch MIDI to Pygame MIDI
{
    1: 
}